<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yongcheng.liuyang.dao.GoodMapper">

<select id="getGoodsPager" resultType="goods">
    select
        id,name,price,picture from goods limit #{skip},#{size}
</select>

<select id="getGoodsCount" resultType="Integer">
    select count(id) from goods
</select>

<insert id="insert" parameterType="Goods">
    insert into goods(name,price,picture)
    values(#{name},#{price},#{picture});
</insert>

<delete id="delete">
    delete from goods where id = #{id}
</delete>

<insert id="insertGoods" parameterType="list">
    insert into goods (name,price,picture)
    values
    <foreach collection="list" index="index" item="good" separator="," open="(" close=")">
        #{good.name},#{good.price},#{good.picture}
    </foreach>
</insert>

</mapper>
